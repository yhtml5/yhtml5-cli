{
  "name": "yhtml5-beautify",
  "version": "0.0.4",
  "license": "MIT",
  "main": "",
  "author": {
    "name": "yhtml5"
  },
  "description": "beautify you code",
  "keywords": [
    "beautify",
    "javascript"
  ],
  "dependencies": {
    "js-beautify": "^1.6.14"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "git": "git checkout dev && git add -A && git commit -m \"auto commit\" && git merge master && git pull && git push && git checkout master && git merge dev && git pull && git push && git checkout dev",
    "postgit": "clear && git status",
    "release": "npm run git && npm publish"
  }
}
